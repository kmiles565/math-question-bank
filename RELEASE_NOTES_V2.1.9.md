# MathBank V2.1.9 更新公告

发布日期：2026-09-03

## 重点修复

- 修复批量导入试卷一次只写入约 15 道题的问题。批量保存改为有界并发，30 道以上题目会持续处理，不再依赖重复点击。
- 修复组卷预览中多幅图片被统一移动到题目末尾的问题。正文、表格单元格和小问中的图片现在保留原始位置。
- 修复解答题留白微调按钮随留白区域移动的问题。控制条固定在留白区域顶部，连续点击时位置和焦点保持稳定。
- 修复 LaTeX 表格超出 A4 页面的问题。常见三列表格会自动换行，表格内图片增加上下间距，避免遮挡横线。
- 强化 Word 导出。`tabular` 会生成真正的 Word 表格，支持单元格图片、`multicolumn` 横向合并、`multirow` 纵向合并、固定列宽和跨页表头。

## 识图改进

- 复杂试卷识图会保留表格结构和题目层级。
- 多图或表格内图片会在原位置留下 `[插图待补: 图1]` 等占位，便于后续手动插图或逐幅使用 TikZ 绘图。
- 仅当整张图片只有一幅位于表格外的独立插图时，才触发自动 TikZ 绘图。

## 验证

- 完整自动化测试：601 项通过。
- PDF、Word 真实导出与渲染已用包含正文图片、表格图片及合并单元格的复杂题验证。

> 版本号：2.1.9
